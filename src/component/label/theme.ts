import styled from "styled-components";
import { black } from "../theme";

export const LabelStyled = styled.div`
    margin-bottom: 24px;
    color: ${black};
    font-weight: 400;
    font-size: 22px;
    line-height: 28px;
`